[project]
name = "disruptor-scoring"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.124.0",
    "langchain>=1.1.3",
    "langchain-groq>=1.1.0",
    "yfinance>=0.2.66",
]
